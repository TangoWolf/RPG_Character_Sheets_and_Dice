<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shaol.char_sheet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="com.example.shaol.char_sheet.data.CharacterProvider"
            android:authorities="com.example.shaol.char_sheet"
            android:exported="false"/>

        <activity android:name=".NewCharActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".ViewCharActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".EditCharActivity" android:parentActivityName=".ViewCharActivity"/>
    </application>

</manifest>